dependencies {
    api project(':instagram-common')
    api project(':instagram-domain')

    implementation 'org.springframework.boot:spring-boot-starter-websocket'
}

tasks.bootJar { enabled = false }
tasks.jar { enabled = true }
